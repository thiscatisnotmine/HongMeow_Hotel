/* Redirection : ใช้เปลี่ยนไปหน้าต่างๆ ตาม 'des' ที่รับเข้ามา ไว้ใช้กับพวก side bar, top bar*/
function redirect(des) {
    window.location.href = des;  // ปรับเปลี่ยนให้เป็น URL ที่คุณต้องการ
    
}
